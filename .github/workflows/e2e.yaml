name: e2e

on:
  schedule:
    - cron: 0 * * * * # every hour

jobs:
  react-template:
    name: Create react starter template
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
      - run: |
          cargo install loco-cli
          LOCO_APP_NAME=react LOCO_TEMPLATE=react loco new
      - run: |
          cargo build
        working-directory: ./react

  rest-api:
    name: Create stateless starter template
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
      - run: |
          cargo install loco-cli
          LOCO_APP_NAME=restapi LOCO_TEMPLATE=rest-api loco new
      - run: |
          cargo build
        working-directory: ./restapi

  lightweight-service:
    name: Create stateless starter template
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
      - run: |
          cargo install loco-cli
          LOCO_APP_NAME=lightweight LOCO_TEMPLATE=lightweight-service loco new
      - run: |
          cargo build
        working-directory: ./lightweight
