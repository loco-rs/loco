# Loco 0.18.0 — Deferred-Item Execution Ledger

**Mandate (2026-07-03):** Execute the items deferred from 0.17.0 **except** #6
(IA docs restructure) and #7 (non-"big-5" feature PRs). Division of labor:
**Sonnet implements, Opus governs** (oversees, tracks, criticizes, gates on
green). Ledgers kept current as work lands.

**Base:** branched `release/0.18.0` off `release/0.17.0` (0.18 items build on the
0.17 tree; 0.17 is unpublished + gated on Sea-ORM 2.0.0 stable).

**Landing policy (unchanged):** local commits only. No push/PR/publish/tag.

## Open governance question (for the user)

0.17.0 has not shipped (gated on Sea-ORM 2.0.0 stable). These items are labeled
"0.18" but land on top of unpublished 0.17. **Version story is deferred to the
end:** keep crates at `0.17.0` during the refactor; at completion decide either
(a) fold into the 0.17.0 release (one combined release), or (b) bump to 0.18.0 as
a distinct follow-up. Flag for user; do not bump versions mid-flight.

## Green gate (run per item; ✅ = clean)

```sh
export DOCKER_HOST="unix://$HOME/.colima/default/docker.sock"
cargo fmt --all --check
cargo clippy --workspace --all-features --tests -- -D warnings
cargo hack check --each-feature
cargo test --all-features
cargo test -p loco-gen
(cd loco-new && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test)
```

Upstream-gated tests (Sea-ORM 2.0.0 stable) remain out of scope: `loco-gen`
`test_migrations_flow`, `loco-new` wizard-matrix + generated-app boot. Noted, not
counted against an item.

## Execution order & rationale

Edition first (front-load the broad mechanical sweep so all later refactor code is
edition-2024-native and green-gated once). Then breaking-API, then architecture,
then structural, then gen, then isolated docs tooling.

| # | Item | Approach (Opus-defined) | Status |
|---|------|-------------------------|--------|
| 4 | **Edition 2024** | `cargo fix --edition` per crate (root ws, xtask, loco-new) + manual review of the semantic changes (RPIT capture, `unsafe` extern, `gen`/`let`-chains, tail-expr drop order). Bump `[workspace.package].edition` + xtask + loco-new; keep rust-version at 1.94 (edition 2024 needs ≥1.85 — satisfied). | ⬜ pending |
| 8 | **Error narrowing** | Audit `src/errors.rs` `pub enum Error` variants; consolidate/remove low-value or leaky variants, keep `#[non_exhaustive]`. Breaking — record every removed/renamed variant + migration note in the ledger + upgrade guide. | ⬜ pending |
| 1 | **bgworker backend dedup** | Extract shared enqueue/dequeue/priority-ordering logic across `pg.rs`/`sqlt.rs` (SQL backends first; Redis ZSET stays distinct). No behavior change; the 0.17 priority tests are the guardrail. | ⬜ pending |
| 3 | **`db.rs`/`config.rs` splits** | Split each oversized file into focused submodules behind an unchanged public surface (re-export from the original module path). No behavior change. | ⬜ pending |
| 2 | **Inflection consolidation** | Route all pluralization/casing through one helper (pick the already-present dep, drop the redundant ones); regenerate loco-gen snapshots; verify identical output. | ⬜ pending |
| 5 | **llms.txt generation pipeline** | Generate `docs-site/static/llms-full.txt` from doc snippets (xtask task) instead of the hand-authored 0.17 file; wire the snipdoc-blocking flip. Output must match/supersede the static file. | ⬜ pending |

## Criticism log (Opus governance findings, per item)

_(appended as each Sonnet deliverable is reviewed)_

## Correspondence / release-notes impact

- **Error narrowing** and **Edition 2024** are user-visible/breaking → append to
  `CHANGELOG.md` (0.17/0.18 section) + `docs-site/content/docs/extras/upgrades.md`
  at the moment each lands.
- Refactors (#1/#2/#3) are internal → CHANGELOG "Changed" one-liners only.
- #5 is docs-infra → note in CHANGELOG "Changed".
