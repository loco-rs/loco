jobs:
  print task:
    task:
      name: "foo"
    cron: "*/5 * * * * *"
    tags:
      - base
      - echo

  write to file:
    shell:
      command: "echo loco >> ./scheduler.txt"
    cron: "*/5 * * * * *"
    tags:
      - base
      - write

