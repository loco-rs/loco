jobs:
  print task:
    run: foo
    cron: "*/5 * * * * *"
    tags:
      - base
      - echo

  write to file:    
    run: "echo loco >> ./scheduler.txt"
    shell: true
    cron: "*/5 * * * * *"
    tags:
      - base
      - write

